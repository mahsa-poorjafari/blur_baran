.field.upload-pic.well      
  =picture.file_field :image, :class => "hide"
  =image_tag (picture.image.present? ? picture.object.image(:medium) : "missing.png"), :class => "enable_file_select" 
  .des-title
    توضیحات به انگلیسی
    =picture.text_area :description, :rows => 2
  .des-title
    توضیحات به فارسی
    =picture.text_area :description_fa, :rows => 2
  
  =picture.link_to_remove image_tag ("Delete_Icon.png"), :class => "remove-task"
.clrear-float
